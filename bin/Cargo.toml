[package]
name = "wordle-entropy-bin"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
#wordle-entropy-core = { path = "../core" }
wordle-entropy-core = { path = "../core", default-features = false }
plotters = "0.3.1"
varpro = "0.3.0"
nalgebra = "0.29"
num-traits = "0.2.14"
num = "0.4.0"
